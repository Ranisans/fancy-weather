const measuringScale = { C: 'C', F: 'F' };

const weatherBlocksClasses = ['first_day', 'second_day', 'third_day'];

const blocksName = ['menuBlock', 'dateBlock', 'currentWeatherBlock', 'mapBlock', 'futureWeatherBlock'];

const languageCode = { en: 'en', ru: 'ru', be: 'be' };

const measurementConst = { metric: 'metric', imperial: 'imperial' };

export {
  measuringScale, weatherBlocksClasses, blocksName, languageCode, measurementConst,
};
